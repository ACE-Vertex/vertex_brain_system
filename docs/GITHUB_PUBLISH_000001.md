# Vertex Brain System — GitHub Publish 000001

Publishes the real local source tree:

`G:\Vertex_Project\Development\vertex_brain_system`

to:

`https://github.com/ACE-Vertex/vertex_brain_system`

## Publication policy

- source-first publication,
- no reconstructed/chat-generated substitute for the local project,
- no build output,
- no release binaries,
- no local model weights,
- no common credentials/secrets,
- staged source is scanned for strong secret/key patterns,
- remote `main` must still be empty for the first push,
- publisher refuses to force-push unexpected remote history,
- local HEAD must equal remote HEAD before success is reported.

Build/test status is deliberately independent from publication status.
BrainSystem may contain active experimental work; preserving source history on GitHub does not imply a runtime/build verification claim.
