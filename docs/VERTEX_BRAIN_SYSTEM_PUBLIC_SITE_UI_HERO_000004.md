# VERTEX BRAIN SYSTEM PUBLIC SITE UI HERO 000004

## Intent

Place the generated Brain System interface image prominently into the public site rather than treating it as a small illustrative asset.

## Changes

- Adds a full-width SYSTEM VISUALIZATION section immediately after the hero.
- Adds the Brain System interface image as the dominant visual element.
- Adds a mathematical observation expression:
  `Ψ_obs(t) = Π_E ∘ O_v ∘ Φ_R(x_t)`
- Adds an academic caption explaining the UI as a constrained projection surface.
- Adds reference commercial valuation:
  `JPY 200,000,000 / 2億円`
- Retains Japanese-first / English-switchable content.
- Retains readable typography and visitor counter.

## Asset

`site/vertex-brain-system-public/assets/vertex-brain-system-neural-observatory.png`
